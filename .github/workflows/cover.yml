name: cover

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: setup dotnet
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.0.x
    - name: cover
      run: |
          cd Runtime
          npm install
          npm run build
          dotnet restore ../Packer/Packer.csproj
          dotnet build ../Generator/Generator.csproj
          dotnet build ../DotNetJS/DotNetJS.csproj
          dotnet publish test/project/Test.csproj
          npm install -g codecov
          npm install -g nyc
          nyc --reporter=lcov -x **/native -x **/bin npm run test
          codecov
