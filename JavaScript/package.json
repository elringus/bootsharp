{
    "scripts": {
        "test": "vitest run --silent --experimental-vm-threads",
        "cover": "npm test -- --coverage.enabled --coverage.include=**/bootsharp/*.mjs --coverage.allowExternal"
    },
    "devDependencies": {
        "typescript": "^5.2.2",
        "@types/node": "^20.6.3",
        "vitest": "^0.34.5",
        "@vitest/coverage-v8": "^0.34.5",
        "ws": "^8.14.2"
    }
}
