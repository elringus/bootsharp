{
    "scripts": {
        "test": "vitest run --silent --experimental-vm-threads",
        "cover": "npm test -- --coverage.enabled --coverage.100 --coverage.include=**/bootsharp/*.mjs --coverage.exclude=**/dotnet.* --coverage.allowExternal",
        "serve": "serve coverage"
    },
    "devDependencies": {
        "typescript": "^5.2.2",
        "@types/node": "^20.6.3",
        "vitest": "^0.34.5",
        "@vitest/coverage-v8": "^0.34.5",
        "ws": "^8.14.2"
    }
}
